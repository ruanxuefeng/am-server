spring:
  datasource:
    druid:
      #基本属性 url、user、password
      url: jdbc:mysql://localhost:3306/am-server?characterEncoding=utf-8
      username: rxf
      #java -cp druid-1.1.9.jar com.alibaba.druid.filter.config.ConfigTools you_password 加密密码命令
      #生成public-key和加密后的密码
      password: Pf4guOQsxXByFuGXn1ifr/3W0wEJuiQX2zwOOfWwE88oWBRSaODpgm7f+eGiiG4vUnrD/RgTehf5A1c0eNo9VA==
      connection-properties: config.decrypt=true;config.decrypt.key=${public-key}
      filters: stat,wall,slf4j
      filter:
        config:
          enabled: true
        slf4j:
          enabled: true
          statement-create-after-log-enabled: false
          statement-log-enabled: true
          statement-executable-sql-log-enable: false
          statement-log-error-enabled: true
          result-set-log-enabled: false
  data:
    mongodb:
      host: localhost
      database: am-server
      option:
        heartbeat-socket-timeout: 1000
        socket-timeout: 3000
        connectTimeout: 3000
        socket-keep-alive: false
        min-heartbeat-frequency: 50
        heartbeat-frequency: 80
        max-connection-idle-time: 30000
        heartbeat-connect-timeout: 1000
  jpa:
    hibernate:
      ddl-auto: none
  redis:
    host: localhost
    port: 6379
    timeout: 1s
    lettuce:
      shutdown-timeout: 5s
      pool:
        max-idle: 20
        min-idle: 1
        max-active: 8
server:
  port: 9527
public-key: MFwwDQYJKoZIhvcNAQEBBQADSwAwSAJBAJQHm7pDoMHVOmmFBs9EPYXIQcg9fS4BkyCUmZqqgTrp65g1EnV1iipCyhAi5VLCKy53/H33TYDv9eFMTAwTWzMCAwEAAQ==
logging:
  level:
    druid:
      sql: debug
local:
  file:
    file-path: D://file/
    uri: /file
    request-url: http://localhost:${server.port}
